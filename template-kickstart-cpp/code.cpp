#include <bits/stdc++.h>

using namespace std;

// *****

auto solve() {
  return 0;
}

// *****

void reparse_test() {}

// *****

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);
  unsigned T;
  cin >> T >> ws;
  for (unsigned t = 0; t < T; ++t) {
    reparse_test();
    cout << "Case #" << (t + 1) << ": " << solve() << '\n';
  }
  return 0;
}
