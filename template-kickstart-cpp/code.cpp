#include <bits/stdc++.h>

using namespace std;

// *****

auto solve() {
  return "0";
}

// *****

void parse_test() {}

void reset_test() {}

// *****

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);
  u64 T;
  cin >> T >> ws;
  for (u64 t = 0; t < T; ++t) {
    parse_test();
    cout << "Case #" << (t + 1) << ": " << solve() << '\n';
    reset_test();
  }
  return 0;
}
