#include <bits/stdc++.h>

using namespace std;

// *****

void solve() {
    int A, B, C, ans = 0;
    cin >> A >> B >> C;
    cout << ans << endl;
}

// *****

int main() {
    ios::sync_with_stdio(false);
    cout.setf(ios::unitbuf);
    unsigned T;
    cin >> T;
    for (unsigned t = 1; t <= T; ++t) {
        solve();
    }
    return 0;
}
